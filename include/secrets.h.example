#ifndef SECRETS_H
#define SECRETS_H

#include <Arduino.h>

// ============================================
// ARCHIVO DE CONFIGURACIÓN DE DIRECCIONES MAC
// ============================================
// 
// INSTRUCCIONES:
// 1. Copia este archivo y renómbralo a "secrets.h"
// 2. Reemplaza las direcciones MAC con las de tus dispositivos
// 3. NO subas el archivo "secrets.h" a GitHub (ya está en .gitignore)
//
// Para obtener la MAC de tu ESP32, descomenta printMacAddress() en setup()

// Direcciones MAC de los receptores
const uint8_t MAC1[] = { 0x00, 0x00, 0x00, 0x00, 0x00, 0x00 };
const uint8_t MAC2[] = { 0x00, 0x00, 0x00, 0x00, 0x00, 0x00 };
const uint8_t MAC3[] = { 0x00, 0x00, 0x00, 0x00, 0x00, 0x00 };

// Array con todas las MACs
const uint8_t* RECEIVERS_MACS[] = { MAC1, MAC2, MAC3 };
const uint8_t RECEIVERS_COUNT = sizeof(RECEIVERS_MACS) / sizeof(uint8_t*);

#endif
